version: "3"

services:
  joeburgess-site:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      - BREEZE_API_KEY=${BREEZE_API_KEY}
    volumes:
      - ./static:/app/static
    ports:
      - "8081:8080"
    networks:
      - joeburgess-network

networks:
  joeburgess-network:
    driver: bridge
